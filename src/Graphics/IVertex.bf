namespace Pile
{
	// TODO: is this useless??
	public interface IVertex
	{
		// Return static format value
		// public VertexFormat Format { get; } // do we need this here at all, or can we just pass it when manipulating the mesh?
	}
}
